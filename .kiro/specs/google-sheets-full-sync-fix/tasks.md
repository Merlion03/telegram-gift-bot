# Implementation Plan

- [x] 1. Write bug condition exploration test
  - **Property 1: Bug Condition** - Google Sheets Full Sync Bug (Updates and Deletes)
  - **CRITICAL**: This test MUST FAIL on unfixed code - failure confirms the bug exists
  - **DO NOT attempt to fix the test or the code when it fails**
  - **NOTE**: This test encodes the expected behavior - it will validate the fix when it passes after implementation
  - **GOAL**: Surface counterexamples that demonstrate the bug exists
  - **Scoped PBT Approach**: Test concrete failing cases - modified records not updated, deleted records not removed
  - Test implementation details from Bug Condition in design:
    - Scenario 1: Record modified in Google Sheets (prize_type changed) but NOT updated in PostgreSQL
    - Scenario 2: Record deleted from Google Sheets (claimed_at IS NULL) but NOT deleted from PostgreSQL
    - Scenario 3: Record deleted from Google Sheets (claimed_at IS NOT NULL) but NOT archived in PostgreSQL
    - Scenario 4: Statistics show updated_records=0, deleted_records=0 even when records are modified/deleted
  - The test assertions should match the Expected Behavior Properties from design
  - Run test on UNFIXED code
  - **EXPECTED OUTCOME**: Test FAILS (this is correct - it proves the bug exists)
  - Document counterexamples found to understand root cause
  - Mark task complete when test is written, run, and failure is documented
  - _Requirements: 1.1, 1.2, 1.3, 1.4_

- [x] 2. Write preservation property tests (BEFORE implementing fix)
  - **Property 2: Preservation** - Existing Sync Behavior Preservation
  - **IMPORTANT**: Follow observation-first methodology
  - Observe behavior on UNFIXED code for non-buggy inputs:
    - New records insertion from Google Sheets
    - Delivery data protection for records with claimed_at IS NOT NULL
    - Backward sync (PostgreSQL → Google Sheets) continues working
    - Graceful degradation on errors (one sheet failure doesn't block others)
  - Write property-based tests capturing observed behavior patterns from Preservation Requirements
  - Property-based testing generates many test cases for stronger guarantees
  - Run tests on UNFIXED code
  - **EXPECTED OUTCOME**: Tests PASS (this confirms baseline behavior to preserve)
  - Mark task complete when tests are written, run, and passing on unfixed code
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5_

- [-] 3. Fix for Google Sheets Full Sync Bug

  - [x] 3.1 Add is_archived field to Prize model
    - Open `telegram-bot/database/models/prize.py`
    - Add `is_archived: Mapped[bool]` field with default=False
    - Add comment: "Маркер архивной записи (удалена из Google Sheets, но данные доставки сохранены)"
    - Add index to __table_args__: `Index('idx_prizes_archived', 'is_archived')` with WHERE clause for archived=true
    - _Bug_Condition: Records deleted from Google Sheets with claimed_at IS NOT NULL should be archived_
    - _Expected_Behavior: is_archived field allows marking deleted records with delivery data_
    - _Preservation: Existing Prize model fields and indexes remain unchanged_
    - _Requirements: 2.3_

  - [x] 3.2 Create database migration for is_archived field
    - Create migration file in `telegram-bot/database/migrations/`
    - Add column: `ALTER TABLE prizes ADD COLUMN is_archived BOOLEAN DEFAULT FALSE NOT NULL;`
    - Create index: `CREATE INDEX idx_prizes_archived ON prizes(is_archived) WHERE is_archived = TRUE;`
    - Add comment: `COMMENT ON COLUMN prizes.is_archived IS 'Маркер архивной записи (удалена из Google Sheets, но данные доставки сохранены)';`
    - _Bug_Condition: Database schema needs is_archived field for archiving mechanism_
    - _Expected_Behavior: Migration adds is_archived field without breaking existing data_
    - _Preservation: Existing data and indexes remain intact_
    - _Requirements: 2.3_

  - [x] 3.3 Add get_prizes_by_sheet method to PrizeRepository
    - Open `telegram-bot/database/repositories/prize_repository.py`
    - Add method `async def get_prizes_by_sheet(self, sheet_name: str) -> List[Prize]`
    - Query: `select(Prize).where(Prize.sheet_name == sheet_name)`
    - Return list of Prize objects for the specified sheet
    - Add logging for performance monitoring (elapsed_ms)
    - Add error handling with DatabaseUnavailableError
    - _Bug_Condition: Need to get all PostgreSQL records for a sheet to perform diff with Google Sheets_
    - _Expected_Behavior: Method returns all records for a sheet to enable diff operation_
    - _Preservation: No changes to existing repository methods_
    - _Requirements: 2.2, 2.3_

  - [x] 3.4 Add batch_delete_prizes method to PrizeRepository
    - Open `telegram-bot/database/repositories/prize_repository.py`
    - Add method `async def batch_delete_prizes(self, prizes_keys: List[Tuple[int, str]]) -> int`
    - Delete records WHERE (telegram_id, code_word) IN prizes_keys AND claimed_at IS NULL
    - Use transaction for atomicity
    - Return count of deleted records
    - Add logging for performance monitoring
    - Add error handling with DatabaseUnavailableError
    - _Bug_Condition: Records deleted from Google Sheets with claimed_at IS NULL should be deleted from PostgreSQL_
    - _Expected_Behavior: Method deletes records without delivery data_
    - _Preservation: No changes to existing repository methods_
    - _Requirements: 2.2, 2.5_

  - [x] 3.5 Add batch_archive_prizes method to PrizeRepository
    - Open `telegram-bot/database/repositories/prize_repository.py`
    - Add method `async def batch_archive_prizes(self, prizes_keys: List[Tuple[int, str]]) -> int`
    - Update records SET is_archived=TRUE WHERE (telegram_id, code_word) IN prizes_keys AND claimed_at IS NOT NULL
    - Use transaction for atomicity
    - Return count of archived records
    - Add logging for performance monitoring
    - Add error handling with DatabaseUnavailableError
    - _Bug_Condition: Records deleted from Google Sheets with claimed_at IS NOT NULL should be archived_
    - _Expected_Behavior: Method archives records with delivery data instead of deleting_
    - _Preservation: Delivery data protection remains intact_
    - _Requirements: 2.3_

  - [x] 3.6 Modify batch_upsert_prizes to return detailed statistics
    - Open `telegram-bot/database/repositories/prize_repository.py`
    - Change return type from `int` to `Dict[str, int]`
    - Use PostgreSQL RETURNING clause to distinguish new vs updated records
    - Return `{'new_records': N, 'updated_records': M}`
    - Keep existing delivery data protection logic (CASE WHEN for claimed_at IS NOT NULL)
    - Update logging to include new_records and updated_records separately
    - _Bug_Condition: Current implementation returns only total count, cannot distinguish new vs updated_
    - _Expected_Behavior: Method returns detailed statistics for new and updated records_
    - _Preservation: Delivery data protection logic remains unchanged_
    - _Requirements: 2.1, 2.4_

  - [x] 3.7 Implement three-phase sync in sync_sheet method
    - Open `telegram-bot/services/sync_service.py`
    - Modify `async def sync_sheet(self, sheet_name: str) -> Dict[str, Any]`
    - **Phase 1: INSERT/UPDATE**
      - Call `batch_upsert_prizes()` for records from Google Sheets
      - Capture `{'new_records': N, 'updated_records': M}`
    - **Phase 2: DELETE**
      - Call `get_prizes_by_sheet(sheet_name)` to get PostgreSQL records
      - Create set of keys from Google Sheets: `{(telegram_id, code_word)}`
      - Find deleted records: records in PostgreSQL but not in Google Sheets
      - Split into two lists:
        - `to_delete`: records with claimed_at IS NULL
        - `to_archive`: records with claimed_at IS NOT NULL
      - Call `batch_delete_prizes(to_delete)`
      - Call `batch_archive_prizes(to_archive)`
      - Capture deleted_count and archived_count
    - **Phase 3: STATS**
      - Aggregate statistics from all phases
      - Return `{'new_records': N, 'updated_records': M, 'deleted_records': K, 'elapsed_seconds': T}`
    - Add logging for each phase
    - Add error handling to maintain graceful degradation
    - _Bug_Condition: isBugCondition(input) - records modified or deleted in Google Sheets_
    - _Expected_Behavior: expectedBehavior(result) - all changes synced correctly with proper statistics_
    - _Preservation: Graceful degradation, delivery data protection, backward sync unchanged_
    - _Requirements: 2.1, 2.2, 2.3, 2.4, 2.5, 3.1, 3.2, 3.3, 3.4, 3.5_

  - [x] 3.8 Update sync_all_sheets to aggregate deleted_records statistics
    - Open `telegram-bot/services/sync_service.py`
    - Modify `async def sync_all_sheets(self) -> Dict[str, Any]`
    - Add `deleted_records` field to stats dictionary initialization
    - Aggregate `deleted_records` from each sheet_stats
    - Update logging to include deleted_records in final statistics
    - _Bug_Condition: Statistics don't include deleted_records count_
    - _Expected_Behavior: Statistics include deleted_records for monitoring_
    - _Preservation: Existing error handling and graceful degradation unchanged_
    - _Requirements: 2.4_

  - [x] 3.9 Verify bug condition exploration test now passes
    - **Property 1: Expected Behavior** - Google Sheets Full Sync Fixed
    - **IMPORTANT**: Re-run the SAME test from task 1 - do NOT write a new test
    - The test from task 1 encodes the expected behavior
    - When this test passes, it confirms the expected behavior is satisfied
    - Run bug condition exploration test from step 1
    - **EXPECTED OUTCOME**: Test PASSES (confirms bug is fixed)
    - Verify all scenarios:
      - Modified records are updated in PostgreSQL with updated_records > 0
      - Deleted records without delivery data are removed from PostgreSQL
      - Deleted records with delivery data are archived (is_archived=true)
      - Statistics correctly show new_records, updated_records, deleted_records
    - _Requirements: 2.1, 2.2, 2.3, 2.4, 2.5_

  - [ ] 3.10 Verify preservation tests still pass
    - **Property 2: Preservation** - Existing Sync Behavior Preserved
    - **IMPORTANT**: Re-run the SAME tests from task 2 - do NOT write new tests
    - Run preservation property tests from step 2
    - **EXPECTED OUTCOME**: Tests PASS (confirms no regressions)
    - Verify all preservation requirements:
      - New records insertion works correctly
      - Delivery data protection for claimed_at IS NOT NULL records
      - Backward sync continues working
      - Graceful degradation on errors
      - No unnecessary UPDATE operations for unchanged records
    - Confirm all tests still pass after fix (no regressions)
    - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5_

- [ ] 4. Checkpoint - Ensure all tests pass
  - Run all bug condition exploration tests - должны пройти
  - Run all preservation property tests - должны пройти
  - Run existing unit tests for PrizeRepository - должны пройти
  - Run existing unit tests for SyncService - должны пройти
  - Verify no regressions in backward sync functionality
  - Ask the user if questions arise
