# План Реализации Исправления

- [x] 1. Написать exploratory property-based тест для выявления бага
  - **Property 1: Fault Condition** - Обработка Канала new_message
  - **КРИТИЧЕСКИ ВАЖНО**: Этот тест ДОЛЖЕН ПРОВАЛИТЬСЯ на неисправленном коде - провал подтверждает существование бага
  - **НЕ ПЫТАТЬСЯ исправить тест или код при провале**
  - **ПРИМЕЧАНИЕ**: Этот тест кодирует ожидаемое поведение - он будет валидировать исправление, когда пройдёт после реализации
  - **ЦЕЛЬ**: Выявить контрпримеры, демонстрирующие баг
  - **Подход Scoped PBT**: Для детерминистичного бага ограничить property конкретными проваливающимися случаями для воспроизводимости
  - Тестовые случаи для проверки:
    - Эмулировать уведомление в канал `new_message` с валидным payload от триггера
    - Проверить, что `createMessageFromPayload()` возвращает `null` (текущее поведение)
    - Проверить, что логируется предупреждение "⚠️ Неизвестный канал: new_message"
    - Подписать клиента на `session_5` → эмулировать уведомление в `new_message` с `session_id: 5`
    - Проверить, что клиент НЕ получает уведомление (текущее поведение)
    - Подписать клиента на `all_messages` → эмулировать уведомление в `new_message`
    - Проверить, что клиент НЕ получает уведомление (текущее поведение)
  - Запустить тест на НЕИСПРАВЛЕННОМ коде
  - **ОЖИДАЕМЫЙ РЕЗУЛЬТАТ**: Тест ПРОВАЛИВАЕТСЯ (это правильно - доказывает существование бага)
  - Задокументировать найденные контрпримеры для понимания корневой причины
  - Отметить задачу выполненной, когда тест написан, запущен и провал задокументирован
  - _Requirements: 1.1, 1.2, 1.3, 1.4_

- [x] 2. Написать preservation property-based тесты (ДО реализации исправления)
  - **Property 2: Preservation** - Обработка Существующих Каналов
  - **ВАЖНО**: Следовать методологии observation-first
  - Наблюдать поведение на НЕИСПРАВЛЕННОМ коде для небагованных входных данных
  - Написать property-based тесты, фиксирующие наблюдаемые паттерны поведения из Preservation Requirements
  - Property-based тестирование генерирует множество тестовых случаев для более строгих гарантий
  - Тестовые случаи для проверки:
    - Наблюдать: уведомления в канал `session_status_change` корректно обрабатываются на неисправленном коде
    - Написать property-based тест: для всех уведомлений в `session_status_change` результат идентичен текущему поведению
    - Наблюдать: уведомления в канал `session_type_change` корректно обрабатываются на неисправленном коде
    - Написать property-based тест: для всех уведомлений в `session_type_change` результат идентичен текущему поведению
    - Наблюдать: прямые уведомления в канал `all_messages` (если используются) корректно обрабатываются
    - Написать property-based тест: для всех прямых уведомлений в `all_messages` результат идентичен текущему поведению
    - Наблюдать: прямые уведомления в каналы `session_*` (если используются) корректно обрабатываются
    - Написать property-based тест: для всех прямых уведомлений в `session_*` результат идентичен текущему поведению
    - Наблюдать: невалидные payload и отсутствующие поля обрабатываются с логированием ошибок
    - Написать property-based тест: обработка ошибок идентична текущему поведению
  - Запустить тесты на НЕИСПРАВЛЕННОМ коде
  - **ОЖИДАЕМЫЙ РЕЗУЛЬТАТ**: Тесты ПРОХОДЯТ (подтверждают базовое поведение для сохранения)
  - Отметить задачу выполненной, когда тесты написаны, запущены и проходят на неисправленном коде
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5, 3.6, 3.7_

- [x] 3. Исправление обработки канала new_message в NotificationRouter

  - [x] 3.1 Добавить обработку канала new_message в метод createMessageFromPayload()
    - Открыть файл `nextjs-app/lib/websocket/server/NotificationRouter.ts`
    - В методе `createMessageFromPayload()` добавить новый case для канала `new_message` перед проверкой кастомных обработчиков
    - Добавить код:
      ```typescript
      // 5. Канал new_message - уведомление от триггера PostgreSQL
      if (channel === 'new_message') {
        return this.createNewMessageFromTrigger(payload);
      }
      ```
    - _Bug_Condition: isBugCondition(notification) где notification.channel === 'new_message'_
    - _Expected_Behavior: createMessageFromPayload() возвращает валидное NewMessageMessage для канала new_message_
    - _Preservation: Обработка каналов session_status_change, session_type_change, all_messages, session_* остаётся неизменной_
    - _Requirements: 1.1, 1.2, 2.1, 2.2_

  - [x] 3.2 Реализовать приватный метод createNewMessageFromTrigger()
    - Создать новый приватный метод `createNewMessageFromTrigger(payload: any): NewMessageMessage | null`
    - Реализовать валидацию структуры payload от триггера (проверка наличия `session_id`, `message_id`, `data`)
    - Реализовать валидацию обязательных полей в `payload.data` (проверка `id`, `session_id`, `message_text`, `created_at`)
    - Реализовать извлечение данных из вложенного объекта `data`
    - Реализовать определение `sender_type` через вызов `determineSenderType(data)`
    - Реализовать создание и возврат объекта `NewMessageMessage` с корректными полями
    - Реализовать обработку ошибок с логированием
    - _Bug_Condition: payload содержит вложенную структуру data от триггера PostgreSQL_
    - _Expected_Behavior: метод корректно парсит payload и создаёт NewMessageMessage_
    - _Preservation: Не влияет на обработку других типов payload_
    - _Requirements: 2.1, 2.2_

  - [x] 3.3 Реализовать вспомогательный метод determineSenderType()
    - Создать новый приватный метод `determineSenderType(data: any): 'user' | 'admin'`
    - Реализовать логику определения типа отправителя: если есть `telegram_id`, то `'user'`, иначе `'admin'`
    - Добавить комментарий с объяснением логики
    - _Bug_Condition: необходимо определить sender_type для корректного отображения сообщения на фронтенде_
    - _Expected_Behavior: метод корректно определяет тип отправителя на основе наличия telegram_id_
    - _Preservation: Не влияет на существующую функциональность_
    - _Requirements: 2.2_

  - [x] 3.4 Реализовать метод getTargetChannels() для маршрутизации к нескольким каналам
    - Создать новый приватный метод `getTargetChannels(channel: string, payload: any): string[]`
    - Реализовать логику для канала `new_message`: возвращать массив `[session_${payload.session_id}, 'all_messages']`
    - Реализовать логику для всех остальных каналов: возвращать массив с одним элементом `[channel]` (маршрутизация 1:1)
    - Добавить комментарии с объяснением логики маршрутизации
    - _Bug_Condition: уведомления из канала new_message должны доставляться нескольким подписчикам_
    - _Expected_Behavior: метод возвращает список целевых каналов для маршрутизации_
    - _Preservation: Для существующих каналов возвращается исходный канал (поведение 1:1 сохраняется)_
    - _Requirements: 2.1, 2.4_

  - [x] 3.5 Модифицировать метод handleNotification() для поддержки множественной маршрутизации
    - Открыть метод `handleNotification(channel: string, payload: string): Promise<void>`
    - После создания `message` через `createMessageFromPayload()` добавить вызов `getTargetChannels(channel, parsedPayload)`
    - Заменить прямую отправку на цикл по целевым каналам:
      ```typescript
      const targetChannels = this.getTargetChannels(channel, parsedPayload);
      for (const targetChannel of targetChannels) {
        await this.broadcastToSubscribers(targetChannel, message);
      }
      ```
    - Убедиться, что существующая логика обработки ошибок и логирования сохранена
    - _Bug_Condition: текущая реализация не поддерживает маршрутизацию к нескольким каналам_
    - _Expected_Behavior: уведомление отправляется всем целевым каналам из списка_
    - _Preservation: Для существующих каналов поведение остаётся идентичным (маршрутизация к одному каналу)_
    - _Requirements: 2.1, 2.3, 2.4_

  - [x] 3.6 Проверить, что exploratory тест теперь проходит
    - **Property 1: Expected Behavior** - Обработка Канала new_message
    - **ВАЖНО**: Перезапустить ТОТ ЖЕ тест из задачи 1 - НЕ писать новый тест
    - Тест из задачи 1 кодирует ожидаемое поведение
    - Когда этот тест проходит, это подтверждает, что ожидаемое поведение достигнуто
    - Запустить exploratory property-based тест из шага 1
    - **ОЖИДАЕМЫЙ РЕЗУЛЬТАТ**: Тест ПРОХОДИТ (подтверждает, что баг исправлен)
    - _Requirements: Expected Behavior Properties из дизайна (2.1, 2.2, 2.3, 2.4)_

  - [x] 3.7 Проверить, что preservation тесты всё ещё проходят
    - **Property 2: Preservation** - Обработка Существующих Каналов
    - **ВАЖНО**: Перезапустить ТЕ ЖЕ тесты из задачи 2 - НЕ писать новые тесты
    - Запустить preservation property-based тесты из шага 2
    - **ОЖИДАЕМЫЙ РЕЗУЛЬТАТ**: Тесты ПРОХОДЯТ (подтверждают отсутствие регрессий)
    - Убедиться, что все тесты всё ещё проходят после исправления (нет регрессий)

- [x] 4. Написать unit-тесты для новой функциональности

  - [x] 4.1 Unit-тест для метода createNewMessageFromTrigger() с валидным payload
    - Создать тест, эмулирующий валидный payload от триггера PostgreSQL
    - Проверить, что метод возвращает корректный объект `NewMessageMessage`
    - Проверить, что все поля заполнены правильно (`id`, `session_id`, `sender_type`, `message_text`, `created_at`, `is_read`)
    - _Requirements: 2.2_

  - [x] 4.2 Unit-тест для метода createNewMessageFromTrigger() с невалидным payload
    - Создать тесты с отсутствующими обязательными полями (`session_id`, `message_id`, `data`)
    - Создать тесты с отсутствующими полями в `data` (`id`, `session_id`, `message_text`, `created_at`)
    - Проверить, что метод возвращает `null` и логирует ошибку
    - _Requirements: 2.2_

  - [x] 4.3 Unit-тест для метода determineSenderType()
    - Создать тест с `telegram_id` в данных → проверить, что возвращается `'user'`
    - Создать тест без `telegram_id` в данных → проверить, что возвращается `'admin'`
    - _Requirements: 2.2_

  - [x] 4.4 Unit-тест для метода getTargetChannels() с каналом new_message
    - Создать тест с каналом `new_message` и `session_id: 5` в payload
    - Проверить, что метод возвращает массив `['session_5', 'all_messages']`
    - _Requirements: 2.1, 2.4_

  - [x] 4.5 Unit-тест для метода getTargetChannels() с другими каналами
    - Создать тесты с каналами `session_status_change`, `session_type_change`, `all_messages`, `session_10`
    - Проверить, что метод возвращает массив с одним элементом - исходным каналом
    - _Requirements: 3.1, 3.2, 3.3, 3.4_

  - [x] 4.6 Unit-тест для маршрутизации к нескольким каналам в handleNotification()
    - Создать mock для `broadcastToSubscribers`
    - Эмулировать уведомление в канал `new_message` с `session_id: 7`
    - Проверить, что `broadcastToSubscribers` вызван дважды: для `session_7` и `all_messages`
    - _Requirements: 2.1, 2.4_

  - [x] 4.7 Unit-тест для граничных случаев
    - Тест с отсутствием подписчиков на целевые каналы → проверить корректное логирование
    - Тест с невалидным `session_id` (null, undefined, не число) → проверить обработку ошибки
    - Тест с пустым payload → проверить обработку ошибки
    - _Requirements: 3.5_

- [x] 5. Написать интеграционные тесты для полного flow

  - [x] 5.1 Интеграционный тест: полный flow от триггера до подписчиков
    - Эмулировать INSERT в таблицу `support_messages` (или эмулировать уведомление от триггера)
    - Проверить, что триггер отправляет уведомление в канал `new_message`
    - Проверить, что `NotificationRouter` обрабатывает уведомление
    - Проверить, что подписчики `session_*` и `all_messages` получают уведомление
    - _Requirements: 2.1, 2.3, 2.4_

  - [x] 5.2 Интеграционный тест: переключение между сессиями
    - Подписать клиента на `session_5` → эмулировать уведомление для `session_id: 5` → проверить получение
    - Отписать клиента от `session_5`
    - Подписать клиента на `session_10` → эмулировать уведомление для `session_id: 10` → проверить получение
    - Проверить, что клиент НЕ получает уведомления для `session_5` после отписки
    - _Requirements: 2.4_

  - [x] 5.3 Интеграционный тест: множественные подписчики
    - Подписать клиента A на `all_messages`
    - Подписать клиента B на `session_5`
    - Подписать клиента C на `session_10`
    - Эмулировать уведомление для `session_id: 5`
    - Проверить, что клиенты A и B получили уведомление, а клиент C не получил
    - Эмулировать уведомление для `session_id: 10`
    - Проверить, что клиенты A и C получили уведомление, а клиент B не получил
    - _Requirements: 2.1, 2.4_

  - [x] 5.4 Интеграционный тест: совместная работа с другими типами уведомлений
    - Подписать клиента на `session_5`, `all_messages`, `status_changes`
    - Эмулировать одновременную отправку уведомлений:
      - Новое сообщение в `new_message` для `session_id: 5`
      - Изменение статуса в `session_status_change` для `session_id: 5`
      - Изменение типа в `session_type_change` для `session_id: 5`
    - Проверить, что все три уведомления корректно обработаны и доставлены соответствующим подписчикам
    - _Requirements: 2.1, 2.3, 3.1, 3.2_

- [x] 6. Checkpoint - Убедиться, что все тесты проходят
  - Запустить все unit-тесты → проверить, что все проходят
  - Запустить все property-based тесты → проверить, что все проходят
  - Запустить все интеграционные тесты → проверить, что все проходят
  - Убедиться, что нет регрессий в существующей функциональности
  - Если возникают вопросы или проблемы, обратиться к пользователю
