# План реализации исправления багов отображения сообщений в чате

## Баг 1: Неправильное отображение отправителя

- [x] 1. Написать исследовательский тест для бага отображения отправителя
  - **Property 1: Fault Condition** - Некорректное преобразование типа отправителя бота
  - **КРИТИЧЕСКИ ВАЖНО**: Этот тест ДОЛЖЕН ПРОВАЛИТЬСЯ на неисправленном коде - провал подтверждает существование бага
  - **НЕ ПЫТАТЬСЯ исправить тест или код, когда он провалится**
  - **ПРИМЕЧАНИЕ**: Этот тест кодирует ожидаемое поведение - он будет валидировать исправление, когда пройдёт после реализации
  - **ЦЕЛЬ**: Выявить контрпримеры, демонстрирующие баг
  - **Подход Scoped PBT**: Для детерминистичных багов ограничить свойство конкретными проваливающимися случаями для воспроизводимости
  - Тестировать детали реализации из секции Fault Condition в дизайне
  - Тестовые утверждения должны соответствовать Expected Behavior Properties из дизайна
  - Запустить тест на НЕИСПРАВЛЕННОМ коде
  - **ОЖИДАЕМЫЙ РЕЗУЛЬТАТ**: Тест ПРОВАЛИТСЯ (это правильно - доказывает существование бага)
  - Задокументировать найденные контрпримеры для понимания первопричины
  - Отметить задачу выполненной, когда тест написан, запущен и провал задокументирован
  - Конкретные проверки:
    - Симулировать WebSocket событие с sender_type='bot'
    - Проверить, что message_type устанавливается в 'from_bot' (провалится на неисправленном коде)
    - Симулировать WebSocket событие с sender_type='admin'
    - Проверить, что message_type устанавливается в 'from_support' (может пройти)
  - _Requirements: 2.1, 2.2, 2.3_

- [x] 2. Написать preservation property тесты для отображения отправителя (ДО реализации исправления)
  - **Property 2: Preservation** - Неизменное отображение сообщений пользователя
  - **ВАЖНО**: Следовать методологии observation-first
  - Наблюдать поведение на НЕИСПРАВЛЕННОМ коде для сообщений от пользователей
  - Написать property-based тесты, фиксирующие наблюдаемые паттерны поведения из Preservation Requirements
  - Property-based тестирование генерирует множество тестовых случаев для более сильных гарантий
  - Запустить тесты на НЕИСПРАВЛЕННОМ коде
  - **ОЖИДАЕМЫЙ РЕЗУЛЬТАТ**: Тесты ПРОЙДУТ (подтверждает базовое поведение для сохранения)
  - Отметить задачу выполненной, когда тесты написаны, запущены и проходят на неисправленном коде
  - Конкретные проверки:
    - Для всех сообщений с sender_type='user' проверить, что message_type='from_user'
    - Проверить, что отображение слева с белым фоном сохраняется
    - Проверить, что форматирование времени не изменяется
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5, 3.6, 3.7_

- [x] 3. Исправление преобразования типа отправителя

  - [x] 3.1 Реализовать исправление логики преобразования
    - Заменить бинарный тернарный оператор на полное условие
    - Явно обработать все три типа: 'user', 'admin', 'bot'
    - Преобразовывать 'admin' в 'from_support'
    - Преобразовывать 'bot' в 'from_bot'
    - Преобразовывать 'user' в 'from_user'
    - Добавить комментарии, объясняющие логику преобразования
    - Добавить fallback для неизвестных типов (по умолчанию 'from_support')
    - _Bug_Condition: isBugCondition1(message) где message.data.sender_type = 'admin' OR 'bot'_
    - _Expected_Behavior: message_type корректно устанавливается для всех типов отправителей_
    - _Preservation: Сообщения от пользователей продолжают отображаться слева с белым фоном_
    - _Requirements: 2.1, 2.2, 2.3, 3.1, 3.2, 3.3, 3.4, 3.5, 3.6, 3.7_

  - [x] 3.2 Проверить, что исследовательский тест теперь проходит
    - **Property 1: Expected Behavior** - Корректное преобразование типа отправителя
    - **ВАЖНО**: Перезапустить ТОТ ЖЕ тест из задачи 1 - НЕ писать новый тест
    - Тест из задачи 1 кодирует ожидаемое поведение
    - Когда этот тест проходит, это подтверждает, что ожидаемое поведение достигнуто
    - Запустить исследовательский тест из шага 1
    - **ОЖИДАЕМЫЙ РЕЗУЛЬТАТ**: Тест ПРОХОДИТ (подтверждает, что баг исправлен)
    - _Requirements: 2.1, 2.2, 2.3_

  - [x] 3.3 Проверить, что preservation тесты всё ещё проходят
    - **Property 2: Preservation** - Неизменное отображение сообщений пользователя
    - **ВАЖНО**: Перезапустить ТЕ ЖЕ тесты из задачи 2 - НЕ писать новые тесты
    - Запустить preservation property тесты из шага 2
    - **ОЖИДАЕМЫЙ РЕЗУЛЬТАТ**: Тесты ПРОХОДЯТ (подтверждает отсутствие регрессий)
    - Убедиться, что все тесты проходят после исправления (нет регрессий)

## Баг 2: Дублирование разделителей дат

- [x] 4. Написать исследовательский тест для бага дублирования разделителей
  - **Property 1: Fault Condition** - Дублирование разделителей дат после перезагрузки
  - **КРИТИЧЕСКИ ВАЖНО**: Этот тест ДОЛЖЕН ПРОВАЛИТЬСЯ на неисправленном коде - провал подтверждает существование бага
  - **НЕ ПЫТАТЬСЯ исправить тест или код, когда он провалится**
  - **ПРИМЕЧАНИЕ**: Этот тест кодирует ожидаемое поведение - он будет валидировать исправление, когда пройдёт после реализации
  - **ЦЕЛЬ**: Выявить контрпримеры, демонстрирующие баг
  - **Подход Scoped PBT**: Ограничить свойство конкретным проваливающимся случаем - сообщения одной даты после перезагрузки
  - Тестировать детали реализации из секции Fault Condition в дизайне
  - Тестовые утверждения должны соответствовать Expected Behavior Properties из дизайна
  - Запустить тест на НЕИСПРАВЛЕННОМ коде
  - **ОЖИДАЕМЫЙ РЕЗУЛЬТАТ**: Тест ПРОВАЛИТСЯ (это правильно - доказывает существование бага)
  - Задокументировать найденные контрпримеры для понимания первопричины
  - Отметить задачу выполненной, когда тест написан, запущен и провал задокументирован
  - Конкретные проверки:
    - Создать массив сообщений с одинаковой датой (например, 3 сообщения сегодня)
    - Вызвать groupMessagesByDate на неисправленном коде
    - Проверить, что создаётся только одна группа для этой даты (провалится на неисправленном коде)
    - Задокументировать, сколько групп создаётся фактически
  - _Requirements: 2.4, 2.5, 2.6_

- [x] 5. Написать preservation property тесты для группировки дат (ДО реализации исправления)
  - **Property 2: Preservation** - Корректная группировка разных дат
  - **ВАЖНО**: Следовать методологии observation-first
  - Наблюдать поведение на НЕИСПРАВЛЕННОМ коде для сообщений разных дат
  - Написать property-based тесты, фиксирующие наблюдаемые паттерны поведения из Preservation Requirements
  - Property-based тестирование генерирует множество тестовых случаев для более сильных гарантий
  - Запустить тесты на НЕИСПРАВЛЕННОМ коде
  - **ОЖИДАЕМЫЙ РЕЗУЛЬТАТ**: Тесты ПРОЙДУТ (подтверждает базовое поведение для сохранения)
  - Отметить задачу выполненной, когда тесты написаны, запущены и проходят на неисправленном коде
  - Конкретные проверки:
    - Для сообщений разных дат проверить, что создаются отдельные группы
    - Проверить, что форматирование дат ("Сегодня", "Вчера", "ДД.ММ.ГГГГ") сохраняется
    - Проверить, что порядок сообщений внутри групп не изменяется
  - _Requirements: 3.3, 3.5_

- [x] 6. Исправление группировки сообщений по датам

  - [x] 6.1 Реализовать исправление функции groupMessagesByDate
    - Изменить логику для использования Map для отслеживания уже созданных групп
    - Вместо последовательного сравнения использовать поиск существующей группы по дате
    - Если группа для даты уже существует, добавлять сообщение в неё
    - Если группы нет, создавать новую
    - Добавить комментарии о том, почему используется Map для группировки
    - Убедиться, что все сообщения одной даты попадают в одну группу
    - _Bug_Condition: isBugCondition2(state) где state.isReloaded = true AND length(state.messages) > 0_
    - _Expected_Behavior: Каждая уникальная дата имеет только один разделитель_
    - _Preservation: Форматирование дат и порядок сообщений сохраняются_
    - _Requirements: 2.4, 2.5, 2.6, 3.3, 3.5_

  - [x] 6.2 Проверить, что исследовательский тест теперь проходит
    - **Property 1: Expected Behavior** - Уникальные разделители дат
    - **ВАЖНО**: Перезапустить ТОТ ЖЕ тест из задачи 4 - НЕ писать новый тест
    - Тест из задачи 4 кодирует ожидаемое поведение
    - Когда этот тест проходит, это подтверждает, что ожидаемое поведение достигнуто
    - Запустить исследовательский тест из шага 4
    - **ОЖИДАЕМЫЙ РЕЗУЛЬТАТ**: Тест ПРОХОДИТ (подтверждает, что баг исправлен)
    - _Requirements: 2.4, 2.5, 2.6_

  - [x] 6.3 Проверить, что preservation тесты всё ещё проходят
    - **Property 2: Preservation** - Корректная группировка разных дат
    - **ВАЖНО**: Перезапустить ТЕ ЖЕ тесты из задачи 5 - НЕ писать новые тесты
    - Запустить preservation property тесты из шага 5
    - **ОЖИДАЕМЫЙ РЕЗУЛЬТАТ**: Тесты ПРОХОДЯТ (подтверждает отсутствие регрессий)
    - Убедиться, что все тесты проходят после исправления (нет регрессий)

## Финальная проверка

- [x] 7. Checkpoint - Убедиться, что все тесты проходят
  - Запустить все unit-тесты
  - Запустить все property-based тесты
  - Запустить все интеграционные тесты
  - Убедиться, что оба бага исправлены
  - Убедиться, что нет регрессий в существующей функциональности
  - Если возникают вопросы, обратиться к пользователю
