# Implementation Plan

- [x] 1. Написать тест исследования bug condition (ПЕРЕД исправлением)
  - **Property 1: Bug Condition** - Backward Sync Перезаписывает Промокод и Инструкцию
  - **КРИТИЧЕСКИ ВАЖНО**: Написать этот property-based тест ПЕРЕД внедрением исправления
  - **ЦЕЛЬ**: Выявить counterexamples, демонстрирующие существование бага
  - **ВАЖНО**: Этот тест ДОЛЖЕН ПРОВАЛИТЬСЯ на неисправленном коде - провал подтверждает наличие бага
  - **НЕ ПЫТАТЬСЯ исправить тест или код когда он провалится**
  - **ПРИМЕЧАНИЕ**: Этот тест кодирует ожидаемое поведение - он будет валидировать исправление, когда пройдёт после реализации
  - **Scoped PBT подход**: Для детерминированного бага ограничить property конкретными проваливающимися случаями для воспроизводимости
  - Тестировать что для записи Prize с prize_type='digital', promo_code='TEST123', instructions='Инструкция', claimed_at NOT NULL:
    - После выполнения backward sync столбец E в Google Sheets содержит 'TEST123' (не перезаписан)
    - После выполнения backward sync столбец F в Google Sheets содержит 'Инструкция' (не перезаписан)
    - Данные доставки записаны в столбцы G-R (12 полей)
  - Запустить тест на НЕИСПРАВЛЕННОМ коде
  - **ОЖИДАЕМЫЙ РЕЗУЛЬТАТ**: Тест ПРОВАЛИТСЯ (это правильно - доказывает существование бага)
  - Задокументировать найденные counterexamples для понимания корневой причины
  - Отметить задачу выполненной когда тест написан, запущен и провал задокументирован
  - _Requirements: 1.1, 1.2, 1.3_

- [x] 2. Написать preservation property тесты (ПЕРЕД внедрением исправления)
  - **Property 2: Preservation** - Forward Sync и Другие Операции Не Изменены
  - **ВАЖНО**: Следовать методологии observation-first
  - Наблюдать поведение на НЕИСПРАВЛЕННОМ коде для не-багованных входных данных:
    - Forward sync корректно читает промокод из столбца E и инструкцию из столбца F
    - Forward sync корректно читает данные доставки из столбцов G-R
    - Backward sync для физических призов корректно записывает данные доставки
    - Backward sync для записей без промокода/инструкции работает корректно
  - Написать property-based тесты, фиксирующие наблюдаемые паттерны поведения из Preservation Requirements
  - Property-based тестирование генерирует множество тестовых случаев для более сильных гарантий
  - Запустить тесты на НЕИСПРАВЛЕННОМ коде
  - **ОЖИДАЕМЫЙ РЕЗУЛЬТАТ**: Тесты ПРОХОДЯТ (подтверждает базовое поведение для сохранения)
  - Отметить задачу выполненной когда тесты написаны, запущены и проходят на неисправленном коде
  - _Requirements: 3.1, 3.2, 3.3, 3.4_

- [x] 3. Исправление перезаписи столбцов E и F при backward sync

  - [x] 3.1 Внедрить исправление в метод _sync_sheet_delivery_data
    - Изменить диапазон обновления с `E{row_id}:P{row_id}` на `G{row_id}:R{row_id}` в строке 976 файла `telegram-bot/services/sync_service.py`
    - Обновить комментарии в коде, описывающие структуру столбцов (E-P → G-R)
    - Обновить docstring метода `sync_delivery_data_to_sheets`: "Обновляет столбцы G-Q (данные доставки) и столбец R (claimed_at)"
    - Проверить соответствие с forward sync: убедиться что `_convert_sheet_data_to_prizes` корректно читает данные из столбцов G-R (индексы 6-17)
    - _Bug_Condition: isBugCondition(input) где input.claimed_at IS NOT NULL AND input.prize_type == 'digital' AND (input.promo_code IS NOT NULL OR input.instructions IS NOT NULL)_
    - _Expected_Behavior: Данные доставки записываются в столбцы G-R, промокод (E) и инструкция (F) остаются нетронутыми_
    - _Preservation: Forward sync, backward sync для физических призов и записей без промокода продолжают работать идентично_
    - _Requirements: 1.1, 1.2, 1.3, 2.1, 2.2, 2.3, 3.1, 3.2, 3.3, 3.4_

  - [x] 3.2 Проверить что bug condition exploration тест теперь проходит
    - **Property 1: Expected Behavior** - Backward Sync Сохраняет Промокод и Инструкцию
    - **ВАЖНО**: Перезапустить ТОТ ЖЕ тест из задачи 1 - НЕ писать новый тест
    - Тест из задачи 1 кодирует ожидаемое поведение
    - Когда этот тест проходит, это подтверждает что ожидаемое поведение удовлетворено
    - Запустить bug condition exploration тест из шага 1
    - **ОЖИДАЕМЫЙ РЕЗУЛЬТАТ**: Тест ПРОХОДИТ (подтверждает что баг исправлен)
    - _Requirements: 2.1, 2.2, 2.3_

  - [x] 3.3 Проверить что preservation тесты всё ещё проходят
    - **Property 2: Preservation** - Forward Sync и Другие Операции Не Изменены
    - **ВАЖНО**: Перезапустить ТЕ ЖЕ тесты из задачи 2 - НЕ писать новые тесты
    - Запустить preservation property тесты из шага 2
    - **ОЖИДАЕМЫЙ РЕЗУЛЬТАТ**: Тесты ПРОХОДЯТ (подтверждает отсутствие регрессий)
    - Подтвердить что все тесты всё ещё проходят после исправления (нет регрессий)
    - _Requirements: 3.1, 3.2, 3.3, 3.4_

- [x] 4. Checkpoint - Убедиться что все тесты проходят
  - Убедиться что все тесты проходят, спросить пользователя если возникнут вопросы
