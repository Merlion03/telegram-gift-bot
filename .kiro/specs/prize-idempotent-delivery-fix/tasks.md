# Implementation Plan

- [x] 1. Написать exploratory тест для Bug Condition (ДО исправления)
  - **Property 1: Bug Condition** - Идемпотентная проверка существования пользователя
  - **КРИТИЧЕСКИ ВАЖНО**: Этот тест ДОЛЖЕН УПАСТЬ на неисправленном коде - падение подтверждает существование бага
  - **НЕ ПЫТАТЬСЯ исправить тест или код, когда он упадёт**
  - **ПРИМЕЧАНИЕ**: Этот тест кодирует ожидаемое поведение - он будет валидировать исправление, когда пройдёт после реализации
  - **ЦЕЛЬ**: Выявить контрпримеры, демонстрирующие существование бага
  - **Подход Scoped PBT**: Для детерминистического бага ограничить property конкретными падающими случаями для воспроизводимости
  - Написать property-based тест для `check_user_exists` в `prize_repository.py`:
    - Создать пользователя с уже полученным призом (claimed_at IS NOT NULL)
    - Вызвать `check_user_exists(telegram_id)`
    - Ожидаемое поведение: должен возвращать True (пользователь существует в таблице призов)
  - Написать property-based тест для полного flow повторного запроса цифрового приза:
    - Создать пользователя с полученным цифровым призом (claimed_at установлен, promo_code заполнен)
    - Симулировать нажатие "Получить приз" и ввод правильного кодового слова
    - Ожидаемое поведение: должен идемпотентно вернуть тот же промокод
  - Написать property-based тест для полного flow повторного запроса физического приза:
    - Создать пользователя с заполненной формой доставки (claimed_at установлен, delivery_data заполнен)
    - Симулировать нажатие "Получить приз" и ввод правильного кодового слова
    - Ожидаемое поведение: должен показать сообщение "✅ Вы уже заполнили данные для получения приза" с кнопками действий
  - Запустить тесты на НЕИСПРАВЛЕННОМ коде
  - **ОЖИДАЕМЫЙ РЕЗУЛЬТАТ**: Тесты УПАДУТ (это правильно - доказывает существование бага)
  - Задокументировать найденные контрпримеры для понимания корневой причины
  - Отметить задачу выполненной, когда тест написан, запущен и падение задокументировано
  - _Requirements: 2.1, 2.2, 2.3, 2.4_

- [x] 2. Написать preservation property тесты (ДО реализации исправления)
  - **Property 2: Preservation** - Сохранение существующего поведения для не-bug случаев
  - **ВАЖНО**: Следовать методологии observation-first
  - Наблюдать поведение на НЕИСПРАВЛЕННОМ коде для не-багованных входных данных
  - Написать property-based тесты, фиксирующие наблюдаемые паттерны поведения из Preservation Requirements
  - Property-based тестирование генерирует множество тестовых случаев для более сильных гарантий
  - **Preservation первичной выдачи цифровых призов**:
    - Наблюдать: пользователь с claimed_at IS NULL получает промокод при первом запросе
    - Написать property: для всех пользователей с claimed_at IS NULL, первичный запрос выдаёт промокод и устанавливает claimed_at
  - **Preservation первичной выдачи физических призов**:
    - Наблюдать: пользователь с claimed_at IS NULL видит форму доставки при первом запросе
    - Написать property: для всех пользователей с claimed_at IS NULL и физическим призом, первичный запрос показывает WebApp форму
  - **Preservation обработки несуществующих пользователей**:
    - Наблюдать: пользователь, отсутствующий в таблице призов, получает сообщение "У вас нет доступных призов"
    - Написать property: для всех telegram_id, не существующих в таблице призов, check_user_exists возвращает False
  - **Preservation валидации кодового слова**:
    - Наблюдать: неправильное кодовое слово отклоняется с сообщением "Кодовое слово введено неверно"
    - Написать property: для всех неправильных кодовых слов, система возвращает ошибку валидации
  - **Preservation установки claimed_at**:
    - Наблюдать: claimed_at устанавливается при первой выдаче цифрового приза
    - Наблюдать: claimed_at устанавливается после заполнения формы доставки для физического приза
    - Написать property: для всех первичных запросов, claimed_at устанавливается корректно
  - **Preservation GDPR согласия**:
    - Наблюдать: пользователи без GDPR согласия получают запрос согласия
    - Написать property: для всех пользователей без согласия, система запрашивает GDPR согласие
  - Запустить тесты на НЕИСПРАВЛЕННОМ коде
  - **ОЖИДАЕМЫЙ РЕЗУЛЬТАТ**: Тесты ПРОХОДЯТ (это подтверждает baseline поведение для сохранения)
  - Отметить задачу выполненной, когда тесты написаны, запущены и проходят на неисправленном коде
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5, 3.6, 3.7_

- [x] 3. Исправление бага идемпотентной выдачи призов

  - [x] 3.1 Исправить метод check_user_exists в prize_repository.py
    - Удалить условие `Prize.claimed_at.is_(None)` из WHERE-клаузы SQL-запроса
    - Изменить запрос с `and_(Prize.telegram_id == telegram_id, Prize.claimed_at.is_(None))` на `Prize.telegram_id == telegram_id`
    - Обновить docstring: "Проверяет наличие пользователя в таблице призов независимо от статуса получения приза (claimed_at)"
    - Удалить или изменить комментарий "claimed_at IS NULL означает, что приз еще не получен"
    - Обновить сообщения логирования для отражения новой логики
    - _Bug_Condition: isBugCondition(input) where userPrizeClaimedAt(input.telegram_id) IS NOT NULL AND check_user_exists returns False_
    - _Expected_Behavior: check_user_exists SHALL return True for any user existing in prizes table regardless of claimed_at value_
    - _Preservation: Preservation Requirements - первичная выдача призов, обработка несуществующих пользователей_
    - _Requirements: 2.1, 2.4, 3.1, 3.2_

  - [x] 3.2 Обновить docstring в prize_service.py
    - Обновить docstring метода `check_user_exists` в `prize_service.py`
    - Изменить описание: "Проверяет наличие пользователя в таблице призов независимо от статуса получения приза (claimed_at)"
    - Убедиться, что описание соответствует новой семантике метода в репозитории
    - _Requirements: 2.1, 2.4_

  - [x] 3.3 Добавить обработку уже заполненной формы доставки в prize_flow_handler.py
    - В методе `handle_code_word_input` после проверки `is_delivery_filled`
    - Если форма заполнена (is_delivery_filled == True):
      - Отправить сообщение `DELIVERY_DATA_ALREADY_FILLED` из `constants/messages.py`
      - Добавить inline клавиатуру через `get_delivery_actions_keyboard(prize_id, webapp_url)` из `keyboards/reply_keyboards.py`
      - Сбросить состояние FSM после отправки сообщения
    - Убедиться, что для цифровых призов метод `_check_prize_postgres` уже корректно обрабатывает повторные запросы
    - Убедиться, что `_mark_prize_claimed_async` не вызывается для уже полученных призов
    - _Bug_Condition: isBugCondition(input) where prize_type == "physical" AND delivery_filled == True_
    - _Expected_Behavior: SHALL show DELIVERY_DATA_ALREADY_FILLED message with action buttons_
    - _Preservation: Preservation Requirements - первичная выдача физических призов, показ WebApp формы_
    - _Requirements: 2.3, 3.7_

  - [x] 3.4 Проверить, что exploratory тест Bug Condition теперь проходит
    - **Property 1: Expected Behavior** - Идемпотентная проверка существования пользователя
    - **ВАЖНО**: Перезапустить ТОТ ЖЕ тест из задачи 1 - НЕ писать новый тест
    - Тест из задачи 1 кодирует ожидаемое поведение
    - Когда этот тест проходит, это подтверждает, что ожидаемое поведение выполнено
    - Запустить bug condition exploratory тест из шага 1
    - **ОЖИДАЕМЫЙ РЕЗУЛЬТАТ**: Тест ПРОХОДИТ (подтверждает, что баг исправлен)
    - _Requirements: 2.1, 2.2, 2.3, 2.4_

  - [x] 3.5 Проверить, что preservation тесты всё ещё проходят
    - **Property 2: Preservation** - Сохранение существующего поведения
    - **ВАЖНО**: Перезапустить ТЕ ЖЕ тесты из задачи 2 - НЕ писать новые тесты
    - Запустить preservation property тесты из шага 2
    - **ОЖИДАЕМЫЙ РЕЗУЛЬТАТ**: Тесты ПРОХОДЯТ (подтверждает отсутствие регрессий)
    - Подтвердить, что все тесты всё ещё проходят после исправления (нет регрессий)
    - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5, 3.6, 3.7_

- [x] 4. Checkpoint - Убедиться, что все тесты проходят
  - Запустить все тесты (exploratory + preservation)
  - Убедиться, что все тесты проходят
  - Если возникают вопросы, обратиться к пользователю
